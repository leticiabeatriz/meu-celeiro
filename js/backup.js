export function exportBackup(state){const payload={app:"Meu Celeiro",version:1,exportedAt:new Date().toISOString(),data:state};const blob=new Blob([JSON.stringify(payload,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`meu-celeiro-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)}
export async function readBackup(file){const parsed=JSON.parse(await file.text());if(!parsed||parsed.app!=="Meu Celeiro"||!parsed.data||!Array.isArray(parsed.data.farms)||!Array.isArray(parsed.data.items)||typeof parsed.data.inventory!=="object"||typeof parsed.data.preferences!=="object")throw new Error("Arquivo de backup inválido.");return structuredClone(parsed.data)}
