import{farmUsed,farmOccupancy,farmFree}from"./calculations.js";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);
const fmt=v=>v?new Intl.DateTimeFormat("pt-BR",{dateStyle:"short",timeStyle:"short"}).format(new Date(v)):"Nunca";
export function renderFarms(state,e,h){const show=e.showArchived.checked;const farms=[...state.farms].sort((a,b)=>a.position-b.position).filter(f=>show||!f.archived);e.body.innerHTML="";if(!farms.length){e.body.innerHTML='<tr class="empty-row"><td colspan="8">Nenhuma farm cadastrada.</td></tr>';return}farms.forEach((farm,index)=>{const used=farmUsed(state,farm.id),free=farmFree(state,farm.id),occ=farmOccupancy(state,farm.id),over=used>farm.barnCapacity,row=document.createElement("tr");row.innerHTML=`<td><div class="order-actions"><button class="mini-button" data-action="up" ${index===0?"disabled":""}>↑</button><button class="mini-button" data-action="down" ${index===farms.length-1?"disabled":""}>↓</button></div></td><td><strong>${esc(farm.name)}</strong></td><td><span class="level-chip">Nv. ${farm.level}</span></td><td>${Number(farm.barnCapacity).toLocaleString("pt-BR")}</td><td><span class="usage-pill ${over?"over":""}">${used}/${farm.barnCapacity} · ${Math.round(occ)}%</span><span class="item-sub">${free>=0?`${free} livres`:`${Math.abs(free)} acima`}</span></td><td>${fmt(farm.lastCheckedAt)}</td><td><span class="state-chip ${farm.archived?"archived":"active"}">${farm.archived?"Arquivada":"Ativa"}</span></td><td><div class="row-actions"><button class="mini-button" data-action="edit">Editar</button><button class="mini-button" data-action="archive">${farm.archived?"Restaurar":"Arquivar"}</button><button class="mini-button danger" data-action="delete">Excluir</button></div></td>`;row.querySelector('[data-action="edit"]').onclick=()=>h.edit(farm.id);row.querySelector('[data-action="archive"]').onclick=()=>h.archive(farm.id);row.querySelector('[data-action="delete"]').onclick=()=>h.delete(farm.id);row.querySelector('[data-action="up"]').onclick=()=>h.move(farm.id,-1);row.querySelector('[data-action="down"]').onclick=()=>h.move(farm.id,1);e.body.append(row)});e.status.textContent=`${farms.length} farm(s) exibida(s).`}
export function normalizePositions(state){state.farms.sort((a,b)=>a.position-b.position).forEach((f,i)=>f.position=i)}
