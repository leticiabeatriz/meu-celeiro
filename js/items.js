import{iconUrl}from"./icons.js";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]);const norm=v=>String(v??"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase();
export function refillItemFilters(state,e){const cats=[...new Set(state.items.map(i=>i.category).filter(Boolean))].sort(),machines=[...new Set(state.items.map(i=>i.machine).filter(Boolean))].sort();const fill=(s,vals)=>{const cur=s.value;s.innerHTML='<option value="">Todas</option>'+vals.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join("");if(vals.includes(cur))s.value=cur};fill(e.category,cats);fill(e.machine,machines)}
export function renderItems(state,e,h){const search=norm(e.search.value),cat=e.category.value,mach=e.machine.value,show=e.showInactive.checked;const items=state.items.filter(i=>show||i.active).filter(i=>!cat||i.category===cat).filter(i=>!mach||i.machine===mach).filter(i=>!search||norm([i.id,i.namePt,i.nameEn,i.category,i.machine].join(" ")).includes(search)).sort((a,b)=>a.unlockLevel-b.unlockLevel||a.namePt.localeCompare(b.namePt,"pt-BR"));e.body.innerHTML="";if(!items.length){e.body.innerHTML='<tr class="empty-row"><td colspan="8">Nenhum item encontrado.</td></tr>';return}items.forEach(item=>{const min=Number(state.preferences[item.id]??state.settings.defaultMinimum),url=iconUrl(item.nameEn),row=document.createElement("tr");row.innerHTML=`<td><div class="item-main"><div class="item-icon-wrap">${url?`<img class="item-icon" src="${url}" alt="">`:""}<span class="icon-fallback">IMG</span></div><div><span class="item-name">${esc(item.namePt)}</span><span class="item-sub">${esc(item.nameEn||"—")}</span><span class="item-id">${esc(item.id)}</span></div></div></td><td><span class="level-chip">Nv. ${item.unlockLevel}</span></td><td>${esc(item.category||"—")}</td><td>${esc(item.machine||"—")}</td><td>${item.maxSalePrice==null?"—":Number(item.maxSalePrice).toLocaleString("pt-BR")}</td><td>${min}</td><td><span class="state-chip ${item.active?"active":"inactive"}">${item.active?"Ativo":"Inativo"}</span></td><td><div class="row-actions"><button class="mini-button" data-action="edit">Editar</button><button class="mini-button" data-action="toggle">${item.active?"Desativar":"Ativar"}</button></div></td>`;const img=row.querySelector(".item-icon");if(img)img.onerror=()=>row.querySelector(".item-icon-wrap")?.classList.add("fallback");row.querySelector('[data-action="edit"]').onclick=()=>h.edit(item.id);row.querySelector('[data-action="toggle"]').onclick=()=>h.toggle(item.id);e.body.append(row)})}
